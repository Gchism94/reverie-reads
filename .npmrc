# pnpm workspace behaviour — this file is TRACKED so installs behave identically for every
# contributor. NEVER put registry auth here: an `_authToken=` line in a tracked file publishes
# your token. Auth belongs in ~/.npmrc. CI's secret scan fails the build on any authToken shape.
auto-install-peers=true
strict-peer-dependencies=false

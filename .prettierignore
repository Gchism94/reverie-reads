node_modules
dist
coverage
playwright-report
test-results
pnpm-lock.yaml
prototype
data
backend
design/from-design-tool
design/Reverie_Theme_Studio.html
*.html

# Self-hosted font mirror — byte-faithful copies of Google Fonts css2 output (scripts/fetch-fonts.mjs
# is the only writer; fontSubsetContract.test.ts guards the content). Reformatting would make every
# re-fetch a spurious diff against upstream.
apps/web/public/fonts/

# Generated source-trial evidence. The writer owns stable JSON/Markdown formatting.
packages/series-source-trial/reports/

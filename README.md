# CLA signatures

Managed by the CLA Assistant Lite GitHub Action (`.github/workflows/cla.yml`). Do not edit by
hand — signatures are written here automatically when a contributor signs the CLA on a pull
request. This branch is intentionally unprotected so the bot's default `GITHUB_TOKEN` can commit
to it directly.
